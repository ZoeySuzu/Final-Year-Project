public enum SpellType { Normal = 0, Fire = 1, Ice = 2, Wind = 3, Electric = 4 };
public enum ReactionType { Horizontal, Vertical, Rotate };
public enum EnemyAIType { basic };
public enum directions { up, down, left, right, forward, back };

public enum Relationship {Nemesis,Enemy,Rival,Aquaintance ,Friend ,BestFriend ,Partner}

public enum ItemType {Equipable, Collectible, Quest }

public enum Reaction { destroy, push };